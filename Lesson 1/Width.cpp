#include <iostream>

/*
    I want to create a program that demonstrates variable assignment and the effects of copy assignment
*/

int main()
{
    /*
    
    // initiall output 5 as the first value
    int width;
    width = 5;

    std::cout << width;

    // after 5 is fed into the console, replace the value with 7 and output it next
    width = 7;

    std::cout << width;

    return 0;

    */

   // combine two steps into one is called initialization with the variable defined as width and the as initial value 5 

   int width {5};

   std::cout << width;
    
}
